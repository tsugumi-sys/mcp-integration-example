# mcp_server

## Run
- `uv run main.py`

## Environment
`python-dotenv` を使って `.env` を読み込みます。

Optional (defaults)
- `APP_SERVER_URL` (http://127.0.0.1:8000)
- `MCP_HOST` (127.0.0.1)
- `MCP_PORT` (9001)

## HTTP Endpoint
- Default URL: `http://127.0.0.1:9001/mcp`
